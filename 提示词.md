你是一名资深 .NET 全栈架构师和工程师。请设计并实现一个“课堂实时答题系统”的可运行 MVP。

请先阅读完整需求，再开始编码。不要擅自改用微服务、消息队列或其他技术栈。遇到不影响主流程的细节，请采用本文给出的默认规则；遇到阻塞性问题，再提出最少量的澄清问题。

==================================================
一、产品目标
==================================================

系统用于课堂随堂练习。

教师按照讲课进度逐题开放试题。约 100 名学员使用手机浏览器加入课堂并答题。教师通过“课堂驾驶舱”实时查看学员在线状态、作答状态、提交进度、选项分布和正确率。

系统包含三个界面：

1. 教师管理端
   - 创建并管理课堂
   - 控制题目开放、截止、公布结果和切换下一题
   - 查看学员名单和个人作答状态
   - 查看实时统计

2. 教师投屏端
   - 适合大屏横屏展示
   - 展示当前题目、倒计时和匿名统计
   - 不显示学员姓名、学号及个人答案

3. 学员移动端
   - 手机优先的响应式 Web 页面
   - 使用课堂码或二维码加入
   - 实时接收教师开放的题目
   - 提交答案并获得明确的成功确认
   - 支持短暂断线、重连和状态恢复

目标并发为单课堂约 100 名学员。第一版采用模块化单体，不采用微服务、RabbitMQ、Kafka或 Kubernetes。

==================================================
二、指定技术栈
==================================================

使用当前项目的技术栈，并在当前项目的拓展（必要时对当前项目进行调整）。

请将后端设计为 ABP 模块化单体。不要使用 ABP 微服务模板。

==================================================
三、工程结构
==================================================

后端至少包含：

- Classroom.Domain.Shared
- Classroom.Domain
- Classroom.Application.Contracts
- Classroom.Application
- Classroom.EntityFrameworkCore
- Classroom.HttpApi
- Classroom.HttpApi.Host
- Classroom.DbMigrator
- Classroom.Tests

前端可以采用一个 React 应用，通过以下路由区分入口：

- /teacher/sessions/:sessionId
- /presentation/sessions/:sessionId
- /student/join
- /student/sessions/:sessionId

也可以在同一 Monorepo 中拆分 teacher-web 和 student-web，但必须共享 DTO、事件契约或生成的 API Client。

核心业务逻辑必须放在领域服务或应用服务中。不要把课堂规则直接写在 Controller、SignalR Hub 或 React 组件里。

==================================================
四、MVP 功能范围
==================================================

1. 教师与题库

- 教师账号登录
- 创建、编辑和删除题目
- 创建试卷并调整题目顺序
- 第一版支持：
  - 单选题
  - 多选题
  - 判断题
  - 简答/填空题
- 为客观题配置正确答案
- 为题目配置解析
- 主观题允许提交，但不自动判分

2. 课堂管理

- 教师从一份试卷创建课堂场次
- 生成课堂码和可分享的加入地址
- 教师可以开始课堂、结束课堂
- 教师逐题开放
- 教师可以截止当前题
- 教师可以公布匿名统计
- 教师可以公布正确答案和解析
- 教师可以切换到下一题
- 当前只能有一道开放题

3. 学员加入

- MVP 默认不要求学员注册完整账号
- 学员通过课堂码加入
- 输入昵称
- 学号字段可选
- 服务端创建 Participant
- 服务端签发仅对当前课堂有效的短期访问令牌
- 一个 Participant 在同一课堂只有一个有效身份
- 同一身份可断线重连
- 重复昵称可以允许，但教师端必须通过 ParticipantId 区分

4. 学员答题

- 只显示当前已开放题
- 截止前允许修改并重新提交
- 以最后一次成功提交的答案为准
- 学员选中答案后先本地暂存
- 点击提交后调用 HTTP API
- 只有收到服务端成功响应后，才显示“提交成功”
- 截止后禁止提交和修改
- 迟到加入者直接进入当前题，不强制补做之前题目
- 倒计时使用服务端返回的绝对 EndsAt
- 最终是否超时由服务端时间判定

5. 教师驾驶舱

实时显示：

- 课堂码
- 当前课堂状态
- 当前题及题号
- 在线人数
- 参与总人数
- 未开始人数
- 作答中人数
- 已提交人数
- 截止未交人数
- 当前题完成率
- 各选项人数与占比
- 客观题正确率
- 平均答题用时
- 学员列表及个人状态
- 学员断线状态
- 最近统计更新时间
- 实时连接状态

控制按钮：

- 开始课堂
- 开放当前题
- 截止当前题
- 公布匿名统计
- 公布正确答案与解析
- 下一题
- 结束课堂

所有按钮必须根据当前课堂状态禁用非法操作。服务端仍需再次校验，不能只依赖前端禁用。

6. 投屏端

只展示：

- 当前题目
- 题目序号
- 倒计时
- 已提交人数/总人数
- 匿名选项分布
- 教师公布后的正确答案和解析

禁止返回或展示：

- 学员姓名
- 学号
- ParticipantId
- 单个学员的答案

==================================================
五、课堂状态机
==================================================

课堂状态：

- Preparing
- Waiting
- Answering
- Explaining
- Finished

题目状态：

- Pending
- Open
- Closed
- StatisticsPublished
- AnswerPublished

合法流程：

Preparing -> Waiting
Waiting -> Answering
Answering -> Closed
Closed -> StatisticsPublished
Closed -> AnswerPublished
StatisticsPublished -> AnswerPublished
StatisticsPublished -> Answering（开放下一题）
AnswerPublished -> Answering（开放下一题）
任意非 Finished 状态 -> Finished

要求：

- 只有教师可以改变课堂状态
- 当前课堂最多一题处于 Open
- Finished 后不能继续开放或提交
- Closed 后不能接收新的答案
- 公布统计与公布答案是两个独立动作
- 所有状态转换在领域层统一校验

每次关键状态变化都递增 ClassroomVersion，例如：

- 开始课堂
- 开放题目
- 截止题目
- 公布统计
- 公布答案
- 切换下一题
- 结束课堂

==================================================
六、核心领域模型
==================================================

至少实现：

1. Question
   - Id
   - Type
   - Stem
   - Options
   - CorrectAnswer
   - Explanation
   - CreatedBy
   - CreationTime

2. Quiz
   - Id
   - Name
   - Description
   - Questions
   - QuestionOrder

3. ClassSession
   - Id
   - QuizId
   - TeacherId
   - ClassroomCode
   - Status
   - CurrentSessionQuestionId
   - Version
   - StartedAt
   - FinishedAt

4. SessionQuestion
   - Id
   - SessionId
   - QuestionId
   - Order
   - Status
   - OpenedAt
   - EndsAt
   - ClosedAt
   - StatisticsPublishedAt
   - AnswerPublishedAt

5. Participant
   - Id
   - SessionId
   - Nickname
   - StudentNumber
   - JoinedAt
   - LastSeenAt
   - OnlineStatus

6. AnswerRecord
   - Id
   - SessionQuestionId
   - ParticipantId
   - AnswerContent
   - FirstStartedAt
   - LastSubmittedAt
   - Revision
   - IsCorrect
   - RequestId

数据库约束至少包含：

- AnswerRecord 对 SessionQuestionId + ParticipantId 唯一
- 已处理的 RequestId 唯一
- ClassroomCode 添加索引
- SessionId + ParticipantId 添加索引
- SessionQuestionId + LastSubmittedAt 添加索引

如果需要保存每次修改历史，可以增加 AnswerRevision；但最终答案仍由 AnswerRecord 表示。

==================================================
七、通信原则
==================================================

可靠业务命令使用 HTTP API：

- 加入课堂
- 获取课堂快照
- 提交或修改答案
- 教师开始课堂
- 开放题目
- 截止题目
- 公布统计
- 公布答案
- 进入下一题
- 结束课堂

SignalR 只负责实时推送和轻量状态通知，不将它作为正式答案持久化的唯一通道。

处理顺序必须是：

1. 验证身份和权限
2. 验证课堂状态
3. 执行数据库事务
4. 数据库事务提交成功
5. 通过 SignalR 推送变化

不能在数据库事务提交前广播成功状态。

==================================================
八、SignalR 设计
==================================================

定义强类型 Hub Client 接口。

分组：

- classroom:{sessionId}:teachers
- classroom:{sessionId}:students
- classroom:{sessionId}:presentation

客户端不能自由决定角色。服务端必须根据令牌和权限判断客户端允许加入哪个分组。

事件至少包含：

- ClassroomStarted
- QuestionOpened
- QuestionClosed
- StatisticsPublished
- AnswerPublished
- ParticipantChanged
- DashboardUpdated
- ClassroomEnded

所有课堂级事件至少携带：

- SessionId
- Version
- ServerTime
- EventId

QuestionOpened 还应包含：

- SessionQuestionId
- Question
- OpenedAt
- EndsAt

注意：

- 开放题目时不能把 CorrectAnswer 提前发送给学员
- 教师公布答案后才发送正确答案和解析
- DashboardUpdated 只发送到教师组
- 投屏组只接收匿名数据
- 学员端不能收到其他学员身份和答案

推荐实现：

- WebSocket 优先
- 开启自动重连
- 如版本支持，可启用 Stateful Reconnect
- 心跳和超时使用合理的生产配置
- 单实例 MVP 不启用 Redis Backplane
- 保留多实例时启用 Redis Backplane 的配置入口

==================================================
九、断线恢复
==================================================

SignalR 连接不是最终状态来源。

实现课堂快照接口：

GET /api/app/class-session/{sessionId}/snapshot

根据身份返回不同快照：

1. 教师快照
   - 完整课堂状态
   - 当前题
   - 学员列表
   - 当前统计
   - 版本号

2. 学员快照
   - 当前课堂状态
   - 当前题
   - EndsAt
   - 本人的提交状态
   - 本人的最终答案
   - 是否已公布统计
   - 是否已公布答案
   - 版本号

3. 投屏快照
   - 匿名课堂状态
   - 当前题
   - 倒计时
   - 匿名统计
   - 版本号

客户端必须在以下情况重新获取快照：

- 首次进入
- SignalR 首次连接成功
- SignalR 重连成功
- 浏览器从后台回到前台
- 收到的 Version 不连续
- 本地状态与服务端事件冲突

==================================================
十、答案提交与幂等
==================================================

提交 DTO 至少包含：

- SessionId
- SessionQuestionId
- ParticipantId 从令牌获取，禁止完全信任请求体
- RequestId
- ClassroomVersion
- Answers
- ClientSubmittedAt

服务端必须验证：

- 令牌是否属于当前课堂
- Participant 是否有效
- 当前题是否已开放
- 当前题是否已经截止
- 课堂是否已经结束
- RequestId 是否已处理
- 答案格式是否符合题型
- ClassroomVersion 是否合理

重复提交相同 RequestId 时，应返回之前的处理结果，不得创建重复记录。

截止前重新提交新的 RequestId：

- 更新 AnswerRecord
- Revision 加一
- LastSubmittedAt 更新为服务端时间
- 重新计算客观题 IsCorrect

客户端时间仅用于诊断，不用于截止判定。

==================================================
十一、在线状态与统计
==================================================

Redis 用于：

- 学员在线状态
- LastSeen 临时数据
- 当前课堂快照缓存
- 当前题统计缓存
- 统计推送节流
- 将来 SignalR 多实例 Backplane

数据库用于：

- 课堂正式状态
- 题目正式状态
- 所有正式答案
- 关键操作时间
- 最终报告数据

教师端统计更新不要在每次提交时全量查询并全量广播。

实现 200～500 毫秒的合并推送机制：

- 每份答案立即写数据库
- 更新 Redis 中的轻量计数
- 在合并窗口内统一向教师端推送 DashboardUpdated
- 教师端个人状态可使用 ParticipantChanged 增量更新
- 定期或在关键节点从数据库重新校准统计

在线状态允许短暂误差，但答案提交状态必须准确。

==================================================
十二、权限和安全
==================================================

教师：

- 使用 ABP Identity/OpenIddict 登录
- 只有课堂创建者或授权教师可控制课堂
- 教师 API 使用权限策略保护

学员：

- 使用课堂范围的短期 JWT
- JWT 至少包含 SessionId、ParticipantId 和 ClientRole
- 令牌不能用于访问其他课堂
- 服务端不能信任前端提交的 SessionId、ParticipantId 或角色声明，必须进行关联校验

投屏：

- 使用只读、短期投屏令牌
- 只能访问匿名快照和匿名事件

其他要求：

- 使用 HTTPS/WSS
- 课堂码具有失效规则
- 对加入课堂、提交答案进行合理限流
- 不在日志中记录完整 JWT
- 不在普通日志中记录敏感身份信息和完整答案
- 正确答案不得提前下发
- 防止越权加入 SignalR 分组
- 配置正确的 CORS
- 前端输出题干和解析时防止 XSS
- 使用参数化查询/EF Core
- API 返回统一错误格式

==================================================
十三、前端交互要求
==================================================

学员端：

- 移动优先
- 清楚显示连接状态：已连接、重连中、离线
- 离线时保留尚未提交的选择
- 提交按钮防止重复点击
- 显示“提交中”
- 服务端确认后显示“提交成功”
- 提交失败时保留答案并允许重试
- 截止后立即禁用编辑
- 倒计时以服务端 EndsAt 和服务器时间差计算
- 页面刷新后通过快照恢复

教师端：

- 优先适配桌面和横屏
- 控制按钮有二次确认，特别是截止、公布答案、结束课堂
- 实时连接中断时给出明显警告
- 统计更新时间可见
- 学员列表可按未答、作答中、已提交、离线筛选
- 图表更新应平滑，不要每次重建全部组件

投屏端：

- 大字号、高对比度
- 自动响应教师状态变化
- 不提供课堂控制按钮
- 不展示实名和个人作答信息

==================================================
十四、建议 API
==================================================

教师：

POST   /api/app/questions
PUT    /api/app/questions/{id}
DELETE /api/app/questions/{id}

POST   /api/app/quizzes
PUT    /api/app/quizzes/{id}

POST   /api/app/class-sessions
POST   /api/app/class-sessions/{id}/start
POST   /api/app/class-sessions/{id}/questions/{questionId}/open
POST   /api/app/class-sessions/{id}/questions/{questionId}/close
POST   /api/app/class-sessions/{id}/questions/{questionId}/publish-statistics
POST   /api/app/class-sessions/{id}/questions/{questionId}/publish-answer
POST   /api/app/class-sessions/{id}/finish
GET    /api/app/class-sessions/{id}/dashboard
GET    /api/app/class-sessions/{id}/snapshot

学员：

POST   /api/public/class-sessions/join
GET    /api/student/class-sessions/{id}/snapshot
POST   /api/student/class-sessions/{id}/answers

投屏：

POST   /api/app/class-sessions/{id}/presentation-token
GET    /api/presentation/class-sessions/{id}/snapshot

可根据 ABP 自动 API Controller 规范调整最终路径，但功能和权限边界必须保留。

==================================================
十五、测试要求
==================================================

至少编写以下测试：

领域单元测试：

- 课堂状态合法转换
- 非法状态转换被拒绝
- 同时只能开放一题
- 截止后不能提交
- 结束后不能开放题目
- 客观题判分
- 多选答案顺序不影响判定

应用集成测试：

- 教师可以创建并开始课堂
- 非课堂教师不能控制课堂
- 学员可以通过课堂码加入
- 无效课堂码不能加入
- 学员不能访问其他课堂
- 相同 RequestId 重试不会产生重复答案
- 截止前新 RequestId 可以更新答案
- 截止后提交被拒绝
- 正确答案在公布前不会出现在学员响应中
- 投屏接口不包含个人信息
- 快照版本正确
- 数据库提交成功后才触发通知

实时测试：

- 学员加入正确 SignalR 分组
- 教师开放题目后学生收到事件
- 学生提交后教师收到统计更新
- 非授权客户端不能加入教师分组
- 重连后重新获取快照
- 版本跳跃触发快照校准

并发测试：

- 模拟 100 名学员在 10 秒内加入
- 模拟 100 名学员在 1～2 秒内提交
- 模拟重复提交和网络重试
- 模拟部分学员断线后重连

目标：

- 正常网络下，大多数开题通知在 1 秒内到达
- 正常提交确认尽量在 500 毫秒内返回
- 教师统计在 1 秒内更新
- 已返回提交成功的答案绝不能丢失
- 重复请求不得产生重复记录

==================================================
十六、开发与交付顺序
==================================================

请按以下阶段实现，完成一个阶段后再继续：

阶段 1：架构与基础设施
- 创建解决方案和项目
- 配置 ABP 模块
- PostgreSQL、Redis 和 Docker Compose
- 数据库迁移
- 认证与基础权限
- Swagger
- 健康检查
- 日志

阶段 2：领域模型
- 实体、聚合根和值对象
- 状态机
- 领域服务
- 数据库约束
- 单元测试

阶段 3：基础业务 API
- 题库
- 试卷
- 创建课堂
- 学员加入
- 快照
- 答案提交与幂等
- 集成测试

阶段 4：SignalR
- 强类型 Hub
- 分组和授权
- 课堂事件
- 重连策略
- 统计合并推送
- 实时测试

阶段 5：前端
- 教师驾驶舱
- 投屏端
- 学员移动端
- API Client
- SignalR Client
- 错误和重连状态

阶段 6：压测与文档
- 100 人集中加入与提交脚本
- 修复并发问题
- README
- 本地启动命令
- 测试账号
- 示例题目和试卷
- 部署说明

==================================================
十七、编码要求
==================================================

- 遵循 ABP 和 ASP.NET Core 的标准实践
- 使用 async/await
- 开启 Nullable Reference Types
- 不使用 dynamic 绕过类型系统
- DTO 与实体分离
- 不直接向前端返回 EF Entity
- 领域层不得依赖 HttpContext、SignalR 或 EF Core
- Hub 不直接执行复杂业务逻辑
- Controller/Hub 不直接操作 DbContext
- 所有写操作必须有明确事务边界
- 重要写操作必须校验并发与状态版本
- 使用 UTC/DateTimeOffset
- 错误信息使用结构化错误码
- 添加必要注释，不写解释显而易见代码的注释
- 不提前搭建微服务或消息队列
- 不为未来假想需求过度抽象

==================================================
十八、交付物
==================================================

最终必须交付：

1. 可运行的完整源代码
2. 数据库迁移
3. Docker Compose
4. README
5. 环境变量示例文件
6. 教师测试账号
7. 示例题库和试卷种子数据
8. 单元测试和集成测试
9. 100 人并发压测脚本
10. OpenAPI 文档
11. 实时事件协议文档
12. 课堂状态机文档
13. 简要部署文档

README 必须包含：

- 系统依赖
- 本地启动步骤
- 数据库迁移步骤
- 测试运行命令
- 前端运行命令
- Docker 启动命令
- 测试账号
- SignalR 路径
- Redis 是否必需
- 常见故障排查
- Nginx WebSocket 代理示例

==================================================
十九、开始执行时的输出格式
==================================================

开始编码前，请先输出：

1. 你选择的具体 .NET、ABP、Node.js 和数据库版本
2. 解决方案目录结构
3. 核心领域模型
4. 状态转换表
5. API 清单
6. SignalR 事件清单
7. 数据库表与关键索引
8. 分阶段实施计划
9. 你识别出的主要技术风险

然后开始阶段 1。不要只给设计说明，必须创建真正可运行的项目、代码、迁移、测试和文档。

如果执行环境无法一次完成全部工作，请优先保证以下主链路可运行：

教师创建课堂
-> 学员通过课堂码加入
-> 教师开放题目
-> 学员实时看到题目
-> 学员提交答案
-> 服务端可靠保存
-> 教师实时看到提交人数与统计
-> 教师截止并公布结果
-> 断线重连后通过快照恢复
